# Integration Architecture Diagram

```
╔══════════════════════════════════════════════════════════════════════════════╗
║                        PHASE 1: DATA COLLECTION (Docker)                     ║
╚══════════════════════════════════════════════════════════════════════════════╝

    ┌─────────────┐
    │  Binance    │
    │  Futures    │  REST API + WebSocket
    │  API        │
    └──────┬──────┘
           │
           ▼
    ┌─────────────────────────────────────────┐
    │  Data Collectors (Python Async)         │
    │  ┌───────────────────────────────────┐  │
    │  │ BinanceFuturesClient              │  │
    │  │ - fetch_ohlcv()                   │  │
    │  │ - fetch_open_interest_hist()      │  │
    │  │ - fetch_funding_rate_history()    │  │
    │  │ - fetch_liquidations()            │  │
    │  │ - fetch_top_trader_ratio()        │  │
    │  │ - fetch_order_book()              │  │
    │  └───────────────────────────────────┘  │
    │                                          │
    │  ┌───────────────────────────────────┐  │
    │  │ OptimizedDataCollector            │  │
    │  │ - Concurrent collection           │  │
    │  │ - Batch inserts                   │  │
    │  │ - Error handling & retries        │  │
    │  └───────────────────────────────────┘  │
    └──────────────┬───────────────────────────┘
                   │
                   ▼
    ┌─────────────────────────────────────────┐
    │  Redis Cache (Deduplication)            │
    │  Port: 6379                             │
    └──────────────┬───────────────────────────┘
                   │
                   ▼
    ┌─────────────────────────────────────────────────────────────┐
    │  TimescaleDB (PostgreSQL + Time-Series)                     │
    │  Port: 5432 (exposed to host)                               │
    │                                                              │
    │  Tables:                                                     │
    │  ┌────────────────┬─────────────┬──────────────────────┐   │
    │  │ ohlcv          │ Hypertable  │ timestamp, symbol,   │   │
    │  │                │             │ open, high, low,     │   │
    │  │                │             │ close, volume        │   │
    │  ├────────────────┼─────────────┼──────────────────────┤   │
    │  │ open_interest  │ Hypertable  │ timestamp, symbol,   │   │
    │  │                │             │ open_interest        │   │
    │  ├────────────────┼─────────────┼──────────────────────┤   │
    │  │ funding_rate   │ Hypertable  │ timestamp, symbol,   │   │
    │  │                │             │ funding_rate         │   │
    │  ├────────────────┼─────────────┼──────────────────────┤   │
    │  │ liquidations   │ Hypertable  │ timestamp, symbol,   │   │
    │  │                │             │ quantity, side       │   │
    │  ├────────────────┼─────────────┼──────────────────────┤   │
    │  │ long_short_    │ Hypertable  │ timestamp, symbol,   │   │
    │  │ ratio          │             │ longShortRatio       │   │
    │  ├────────────────┼─────────────┼──────────────────────┤   │
    │  │ order_book     │ Hypertable  │ timestamp, symbol,   │   │
    │  │                │             │ price, quantity, side│   │
    │  └────────────────┴─────────────┴──────────────────────┘   │
    │                                                              │
    │  Compression: Auto compression policies enabled             │
    │  Retention: Continuous aggregation for efficient queries    │
    └──────────────────────────┬───────────────────────────────────┘
                               │
                               │ PostgreSQL Connection
                               │ (localhost:5432 from host machine)
                               │
                               ▼
╔══════════════════════════════════════════════════════════════════════════════╗
║                    BRIDGE LAYER: MarketDataLoader                            ║
╚══════════════════════════════════════════════════════════════════════════════╝

    ┌─────────────────────────────────────────────────────────────┐
    │  features/data_loader.py                                    │
    │                                                              │
    │  class MarketDataLoader:                                    │
    │                                                              │
    │    def load_all_data(symbol, timeframe, start, end):        │
    │        """                                                   │
    │        Queries TimescaleDB and merges all tables            │
    │        """                                                   │
    │        # 1. Query ohlcv table                               │
    │        ohlcv_df = query("SELECT * FROM ohlcv WHERE ...")    │
    │                                                              │
    │        # 2. Query open_interest table                       │
    │        oi_df = query("SELECT * FROM open_interest ...")     │
    │                                                              │
    │        # 3. Query funding_rate table                        │
    │        funding_df = query("SELECT * FROM funding_rate ...")│
    │                                                              │
    │        # 4. Query liquidations table (aggregated)           │
    │        liq_df = query("SELECT timestamp, SUM(...) ...")     │
    │                                                              │
    │        # 5. Query long_short_ratio table                    │
    │        ls_df = query("SELECT * FROM long_short_ratio ...")  │
    │                                                              │
    │        # 6. Merge all on timestamp                          │
    │        merged = ohlcv_df.merge(oi_df, ...)                  │
    │                        .merge(funding_df, ...)              │
    │                        .merge(liq_df, ...)                  │
    │                        .merge(ls_df, ...)                   │
    │                                                              │
    │        return merged  # Single DataFrame                    │
    │                                                              │
    └──────────────────────────┬───────────────────────────────────┘
                               │
                               │ Merged DataFrame
                               │ Columns: timestamp, open, high, low, close,
                               │          volume, open_interest, funding_rate,
                               │          liq_volume, liq_count, longShortRatio
                               │
                               ▼
    ┌─────────────────────────────────────────────────────────────┐
    │  Data Preparation for Phase 2                               │
    │                                                              │
    │  # Split merged DataFrame into separate DataFrames          │
    │  ohlcv_df = merged[['timestamp', 'o', 'h', 'l', 'c', 'v']]  │
    │  oi_df = merged[['timestamp', 'open_interest']]             │
    │  funding_df = merged[['timestamp', 'funding_rate']]         │
    │  liq_df = merged[['timestamp', 'liq_volume', 'liq_count']]  │
    │  ls_df = merged[['timestamp', 'longShortRatio']]            │
    │                                                              │
    └──────────────────────────┬───────────────────────────────────┘
                               │
                               │ Separate DataFrames
                               │
                               ▼
╔══════════════════════════════════════════════════════════════════════════════╗
║                   PHASE 2: ML FEATURE ENGINEERING (p2_mlFeature)             ║
╚══════════════════════════════════════════════════════════════════════════════╝

    ┌─────────────────────────────────────────────────────────────┐
    │  FeatureEngineer.engineer_all_features()                    │
    │                                                              │
    │  Input: ohlcv_df, oi_df, funding_df, liq_df, ls_df         │
    │                                                              │
    │  ┌────────────────────────────────────────────────────────┐ │
    │  │  OI Features (25+)                                     │ │
    │  │  - oi_pct_change_1, _5, _20                           │ │
    │  │  - oi_velocity_1h, _4h, _24h                          │ │
    │  │  - oi_macd, oi_macd_signal, oi_macd_histogram         │ │
    │  │  - oi_price_divergence_20, _48, _288                  │ │
    │  │  - oi_zscore, oi_percentile_100                       │ │
    │  │  - oi_bb_upper, oi_bb_lower, oi_bb_width             │ │
    │  └────────────────────────────────────────────────────────┘ │
    │  ┌────────────────────────────────────────────────────────┐ │
    │  │  Price Features (30+)                                  │ │
    │  │  - returns_1, log_return_1                            │ │
    │  │  - rsi_14, rsi_50                                     │ │
    │  │  - macd, macd_signal, macd_histogram                  │ │
    │  │  - bb_upper, bb_lower, bb_position                    │ │
    │  │  - atr_14, natr                                       │ │
    │  │  - sma_10, _20, _50, _200                            │ │
    │  │  - ema_12, ema_26                                     │ │
    │  └────────────────────────────────────────────────────────┘ │
    │  ┌────────────────────────────────────────────────────────┐ │
    │  │  Volume Features (20+)                                 │ │
    │  │  - volume_sma_20, volume_ema_20                       │ │
    │  │  - volume_zscore, volume_ratio                        │ │
    │  │  - obv, cmf, mfi                                      │ │
    │  │  - vwap, volume_price_trend                           │ │
    │  └────────────────────────────────────────────────────────┘ │
    │  ┌────────────────────────────────────────────────────────┐ │
    │  │  Funding Features (10+)                                │ │
    │  │  - funding_change, funding_change_24h                 │ │
    │  │  - cumulative_funding_7d, _30d                        │ │
    │  │  - funding_zscore, funding_percentile                 │ │
    │  │  - funding_extreme_positive, _negative                │ │
    │  └────────────────────────────────────────────────────────┘ │
    │  ┌────────────────────────────────────────────────────────┐ │
    │  │  Liquidation Features (10+)                            │ │
    │  │  - liq_volume_1h, liq_count_1h                        │ │
    │  │  - liq_spike, liq_momentum                            │ │
    │  │  - liq_long_volume, liq_short_volume                  │ │
    │  └────────────────────────────────────────────────────────┘ │
    │  ┌────────────────────────────────────────────────────────┐ │
    │  │  L/S Ratio Features (5+)                               │ │
    │  │  - ls_ratio_change_1h, _4h, _24h                      │ │
    │  │  - ls_ratio_percentile                                │ │
    │  │  - ls_ratio_extreme_long, _short                      │ │
    │  └────────────────────────────────────────────────────────┘ │
    │  ┌────────────────────────────────────────────────────────┐ │
    │  │  Time Features (10+)                                   │ │
    │  │  - hour_sin, hour_cos                                 │ │
    │  │  - day_sin, day_cos                                   │ │
    │  │  - is_asia_session, is_eu_session, is_us_session      │ │
    │  └────────────────────────────────────────────────────────┘ │
    │  ┌────────────────────────────────────────────────────────┐ │
    │  │  Interaction Features (10+)                            │ │
    │  │  - oi_volume_interaction                              │ │
    │  │  - oi_price_strength                                  │ │
    │  │  - funding_oi_divergence                              │ │
    │  └────────────────────────────────────────────────────────┘ │
    │                                                              │
    │  Output: DataFrame with 115+ features                       │
    │                                                              │
    └──────────────────────────┬───────────────────────────────────┘
                               │
                               │ 115+ Features
                               │
                               ▼
    ┌─────────────────────────────────────────────────────────────┐
    │  TargetEngineer.create_classification_target()              │
    │                                                              │
    │  Input: features_df, horizon=48, threshold=0.01             │
    │                                                              │
    │  Logic:                                                      │
    │    future_return = (price[t+48] - price[t]) / price[t]      │
    │                                                              │
    │    if future_return > 0.01:                                 │
    │        target = 2  # LONG                                   │
    │    elif future_return < -0.01:                              │
    │        target = 0  # SHORT                                  │
    │    else:                                                     │
    │        target = 1  # NEUTRAL                                │
    │                                                              │
    │  Output: features_df + 'target' column                      │
    │                                                              │
    └──────────────────────────┬───────────────────────────────────┘
                               │
                               │ Features + Target
                               │
                               ▼
    ┌─────────────────────────────────────────────────────────────┐
    │  Feature Selection (utils.select_features_combined)         │
    │                                                              │
    │  Step 1: Remove low-variance features (threshold=0.001)     │
    │          115 features → 110 features                        │
    │                                                              │
    │  Step 2: Remove highly correlated features (threshold=0.9)  │
    │          110 features → 85 features                         │
    │                                                              │
    │  Step 3: Select top N by importance (RandomForest)          │
    │          85 features → 50 features                          │
    │                                                              │
    │  Output:                                                     │
    │    - X_selected (50 features)                               │
    │    - importance_scores (ranking)                            │
    │                                                              │
    └──────────────────────────┬───────────────────────────────────┘
                               │
                               │ Top 50 Features
                               │
                               ▼
    ┌─────────────────────────────────────────────────────────────┐
    │  Time-Series Split (utils.time_series_split)                │
    │                                                              │
    │  NO SHUFFLING! Maintains temporal order.                    │
    │                                                              │
    │  ┌────────────────────────────────────────────────────────┐ │
    │  │  Train Set (60%)                                       │ │
    │  │  timestamp: 2024-10-01 to 2024-10-18                  │ │
    │  └────────────────────────────────────────────────────────┘ │
    │  ┌────────────────────────────────────────────────────────┐ │
    │  │  Validation Set (20%)                                  │ │
    │  │  timestamp: 2024-10-18 to 2024-10-25                  │ │
    │  └────────────────────────────────────────────────────────┘ │
    │  ┌────────────────────────────────────────────────────────┐ │
    │  │  Test Set (20%)                                        │ │
    │  │  timestamp: 2024-10-25 to 2024-11-09                  │ │
    │  └────────────────────────────────────────────────────────┘ │
    │                                                              │
    └──────────────────────────┬───────────────────────────────────┘
                               │
                               ▼
    ┌─────────────────────────────────────────────────────────────┐
    │  OUTPUT FILES                                                │
    │                                                              │
    │  📁 data/phase2_features_complete.parquet                   │
    │     - All 115+ features                                     │
    │     - Size: ~10-50MB                                        │
    │     - Use for: Feature analysis, exploration               │
    │                                                              │
    │  📁 data/phase2_features_selected.parquet  ← USE THIS!     │
    │     - Top 50 selected features + target                     │
    │     - Size: ~2-5MB                                          │
    │     - Use for: ML model training                            │
    │                                                              │
    │  📁 data/feature_importance.csv                             │
    │     - Feature rankings with importance scores               │
    │     - Use for: Understanding feature relevance              │
    │                                                              │
    └──────────────────────────┬───────────────────────────────────┘
                               │
                               ▼
╔══════════════════════════════════════════════════════════════════════════════╗
║                      PHASE 3: ML MODEL TRAINING (Next Step)                  ║
╚══════════════════════════════════════════════════════════════════════════════╝

    Load: data/phase2_features_selected.parquet
           ↓
    Train: XGBoost, LightGBM, Neural Networks
           ↓
    Evaluate: Validation accuracy, precision, recall
           ↓
    Deploy: Live trading system


═══════════════════════════════════════════════════════════════════════════════

                            CONNECTION DETAILS

═══════════════════════════════════════════════════════════════════════════════

Host Machine (Windows)
    │
    │ Run: python examples/phase1_to_phase2_integration_demo.py
    │
    ├─► sys.path.insert(0, 'C:/path/to/p2_mlFeature')  # Add Phase 2
    │
    ├─► from features.data_loader import MarketDataLoader  # Phase 1 bridge
    │
    └─► DATABASE_URL = "postgresql://futures_user:***@localhost:5432/futures_data"
              │
              │ TCP/IP Connection
              │
              ▼
    Docker Network: futures-network
        │
        └─► Container: futures_db (TimescaleDB)
                │
                └─► Port Mapping: 5432:5432
                      │
                      └─► Accessible from host: localhost:5432


═══════════════════════════════════════════════════════════════════════════════

                          KEY FILES REFERENCE

═══════════════════════════════════════════════════════════════════════════════

Phase 1 (p1_dataCollection):
├── features/data_loader.py                    ← Bridge to database
├── examples/phase1_to_phase2_integration_demo.py  ← Run this!
├── config.yaml                                 ← Configuration
├── P1_TO_P2_INTEGRATION_GUIDE.md             ← Technical guide
├── QUICKSTART_P1_TO_P2.md                     ← Quick start
└── README_INTEGRATION.md                       ← This summary

Phase 2 (p2_mlFeature):
├── features/feature_engineer.py                ← 115+ features
├── features/target_engineer.py                 ← Create targets
├── features/feature_store.py                   ← Redis caching
├── utils/feature_selection.py                  ← Feature selection
├── utils/data_split.py                         ← Time-series split
└── example_usage.py                            ← Phase 2 examples


═══════════════════════════════════════════════════════════════════════════════

                              STATUS: ✅ READY

Phase 1: Data Collection (Docker) - RUNNING
Phase 2: Feature Engineering (p2_mlFeature) - INTEGRATED
Phase 3: ML Model Training - NEXT STEP

You can now train ML models using the engineered features! 🚀

═══════════════════════════════════════════════════════════════════════════════
```
